"""Starter challenge. Implement this before comparing approaches with peers."""


def validate_citation(response: dict, expected_citation: str) -> bool:
    """Return True only for the expected, non-empty citation.

    Keep this deterministic: no model or evaluator is required.
    """
    raise NotImplementedError("Your challenge: validate response['citation'] safely")
